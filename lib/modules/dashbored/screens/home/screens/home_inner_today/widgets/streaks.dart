import 'package:flutter/material.dart';

class Streaks extends StatelessWidget {
  const Streaks({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 10,
      width: 2,
      color:const Color(0xff5A7B51),
      
    );
  }
}